# AGENTS.md

このファイルはリポジトリ全体の **デフォルトルール** です。すべての AI コーディングツール
（Claude Code / Codex / Cursor / Antigravity ほか）がこのファイルを参照します。

## このファイルの権威

- グローバル設定や個人の慣習とこのファイルが矛盾する場合、**常にこのファイルを優先** する。
- より深いディレクトリに `AGENTS.md` がある場合、そのディレクトリ配下では **近い方が優先**。
  書いていない項目だけ、このルートを継承する。

## 普遍ルール（言語・クラウドに依存しない）

どの案件・どの言語でも常に守る、土台のルール。

- 秘密情報（API キー等）はコミットしない。`.env*` は Git 管理外。
- 変更後は、**その案件で定義されたチェック**（型 / Lint / テスト）を通してからコミットする。
- 既存のコードスタイル・構成に合わせる。**指示のない大規模リファクタリングは禁止**。
- 依存ライブラリの追加やパッケージマネージャーの混在は勝手にやらない（理由を添えて提案する）。
- コミットは小さく、説明的に。

## セッション開始の儀式（ロードマップ）

> ## ⚠️ 引き継ぎ（2026-07-20 記録・**最優先**）
>
> **次セッションは、通常フローに入る前にまずこの件を片付ける。** 完了したらこの引き継ぎ節を削除すること。
>
> ### 何が起きたか（＝直す理由）
> ロードマップに「状態/作業モデル＋受入ゲート」を入れたが、**検証が“自己採点”のままで、今の緑（done）は信用できない**ことが判明した。現状確認で以下が事実：
> - **CI（機械の審判）が無い**（`.github/workflows` 不在）。
> - **判定用サブエージェントが無い**（`.claude/agents` 不在）。
> - **`G-1-3 品質チェック緑` は偽の緑**：`pnpm -r test` の中身が `echo "(web) test: none yet"` で、テストが0個でも必ず「成功」する。`lint` も `packages/ui` は未設定でスルー。＝問題ゼロの試験に合格しているだけ。
> - **`G-1-1 土台=done` は自己申告**：機械も独立審判も無く、「AIがそう言った」だけ。我々の基準では done ではない。
> - 結論：**地図は緑・現実は空**。「運用できる環境」には検証の仕組み自体が含まれるのに、それが丸ごと無い。
>
> ### 今セッションで合意した原則（これに沿って直す）
> - **二層構造**：`言語化`（何/なぜ＝状態・criteria・verify、最初に固定・git・不変再帰）→ `手順化`（どう＝作業、都度・使い捨て）。境目は `verify`。ゲートは「手順化→言語化」への一方通行（evidence を verify が食って状態が満たされる）。
> - **検証は本人に採点させない＝2階建て**：①機械で白黒つく所（型/テスト/ビルド/URL到達）は**機械（CI）**が判定。②機械で白黒つかない所（画面が正しい等）だけ、**独立サブエージェント**が判定（＝作業した本人以外・報告を鵜呑みにせず自分で確かめ直す・敵対的に落としにいく）。
> - **基準は実行者が書き換え不能**：`criteria`/`verify` は着手前に固定。変更は**人間（マスター）承認**を PR/diff で通す（bar を下げていないか人が見る）。
> - **evidence は偽造不能な外部事実**：CI の run URL / 変更を実際に含む commit SHA / デプロイID / 第三者が叩く公開URL。**「スクショ保存した」等の自己申告を証拠にしない。**
> - **粒度の線**：各葉の状態に、実行可能な `verify` が1つ書けるところまで割る。書けたら止め、その下（操作）は着手時に展開。個数は必要十分性から導出（設計しない）、粒度だけ最初に選ぶ。
>
> ### 最初にやること（この順番）
> 1. **ロードマップを正直に戻す**：`G-1-1` の done を剥がす。`G-1`（土台/環境が整っている）の受入条件に「機械の審判（CI）が居る」「本物のテストがある（echo を実テストへ）」「基準は実行者が書き換えられない」を追加。**偽の緑を消す**。
> 2. **機械の審判（CI）を立てる**：GitHub Actions で `pnpm -r typecheck`＋（空でない）`test`＋`build` を回す。以後、G-1 系の evidence はその **run URL / commit SHA** を指す（AIの「できました」を証拠から追放）。
> 3. **AGENTS.md に独立検証ルールを明文化**：基準変更は人間承認、done 判定は機械／独立サブエージェント、evidence は外部事実、を恒久ルール化（この引き継ぎの原則を本文へ昇格）。
>
> ### 現在の事実（現在地）
> - ブランチ `claude/hai-buj96e`（PR #2 は main へマージ済み＝**次も最新 main から同名で切り直す**）。最新コミット `3161758`＝ゴール中心ツリー。
> - ロードマップのゴール＝**「このリポからアプリを本番運用できる環境にする」**。`meta.active = G-1-2`。ただし上記のとおり **G-1-1 の done と G-1-3 の緑は現基準では未達扱いが正しい**。
> - この引き継ぎを書いた時点で、修正は**未着手**（ユーザー指示で次セッションへ送り）。

- ロードマップの**正は `docs/roadmap.html` 内の JSON**（`<script id="roadmap-data">`）。日々の更新は**その JSON だけ**を編集する。判定・受入ゲートは末尾の描画エンジンが担い、**モデルを変える時だけ**エンジンを触る。
- **セッション開始時**：まず `docs/roadmap.html` の `meta.active`（現在地）と `meta.next`（次の一手）を読み、ユーザーに**現在地と次の一手を報告してから**作業に入る。

### ノードは2種（上流＝状態／下流＝作業）

- **状態（`kind:"state"`）**：満たしたい結果。`criteria`（受入条件）を持つ。上流。
- **作業（`kind:"work"`）**：実行する操作。`status`＋`commit`/`done_at` を持つ。下流。
- 層数・タイプ名は固定しない（再帰）。**作業を進めると状態が満たされる**構造にする。

### 最初に決める3点 ／ 都度でよい1点

- **最初に固める（AI・セッションで変えない）**：①やる事＝状態、②出来た基準＝`criteria.text`、③**検証方法＝`criteria.verify`**。
- **都度でよい（変わってよい）**：作業の**道順**（どう作るか）。上流の状態は先に描き切り、下流の作業は**着手時に**割る（遅延展開）。
- **検証方法を後で決めない**：後付けは「自分の成果が通る検証」に歪む。`verify` は**実行可能な手順**で書く（例：`pnpm -r typecheck && pnpm -r lint && pnpm -r test`）。1コマンド化できない検証は、具体的な観察対象＋証拠（スクショ等）で固定する。**判断ではなく手順を固定する。**

### 完了ゲート（品質担保の心臓）

- **作業**：実行して記録（`commit`/`done_at`）が残れば `done`。
- **状態**：子が全部 `done` でも `done` にしない。`criteria` を**全て検証し `evidence` が付いて初めて** `done`。子は済だが未検証なら `doing`（＝検証待ち）のまま。**作業→検証→状態成立の一方通行。**
- **証拠の縛り**：`evidence` は commit／テスト出力／スクショ等の**実記録**を指す。「◯◯レビュー.md が存在する」は証拠と認めない。
- **検証は都度（ゲート）**：完走後にまとめてテストしない。状態の子が全部埋まった時点で、その状態の**固定された `verify`** を回してゲートを開ける。失敗しても原因はその状態の作業に局所化される。
- `todo`/`doing`/`done`/`blocked` の4値。**親（状態）の状態は子＋受入ゲートから自動導出**（手動で done にしない）。

- **割り方**：目的で割る。コミット1個サイズになったら止める。
- **二重管理をしない**：完了の真実は git。ロードマップは git を参照する形にし、事実と食い違わせない。

## スタックは「ここでは決めない」

このルートは **言語・フレームワーク・クラウドを固定しない**。
案件によって前提が全く変わる（例: AWS + Python、Azure + .NET、Next.js + Vercel）ため、
固定デフォルトを置くと **誤ったスタックが継承されて事故る**。

→ スタックは案件ごとに、下記フローで決め、その案件の `AGENTS.md` に宣言する。

## 新規案件の初期化フロー（デフォルト手順）

新しい案件・アプリを始めるとき、スタックを勝手に仮定せず、**必ず次の順で進める**。

1. **ヒアリング** — 下記「決めるべき必要項目」を、まず *制約* から確認する。
   （クラウド指定はあるか / 言語指定は / 既存システム連携は / 顧客・チームの縛りは）
2. **リサーチ** — 確定した制約の範囲で、その年の最新ベスト構成を調べる。
3. **提案** — 具体的なスタックを **1 案（＋根拠、＋代替 1 つ）** で提示する。
4. **承認** — ユーザーの明示的な OK を取る。**黙って進めない**。
5. **設定** — 承認された構成を、その案件の `AGENTS.md`（`presets/_TEMPLATE.md` を複製）に
   記載し、必要な雛形を作る。

### 決めるべき必要項目（ヒアリングのチェックリスト）

- クラウド / ホスティング（AWS / Azure / GCP / Vercel / 自前 …）
- 言語 / ランタイム
- フレームワーク
- パッケージ / 依存管理
- DB + データアクセス（ORM / クライアント）
- 認証
- IaC / デプロイ方法
- テスト
- CI
- Lint / フォーマッタ

未確定の項目は「未定」と明記し、決まり次第このリストを埋める。

## 配下ごとの調整（オーバーライド）

サブディレクトリ（または各案件リポジトリ）に `AGENTS.md` を置くと、そこで実スタックを宣言する。
書いていない項目はこのルートの普遍ルールを継承する。

- 案件用の雛形: `presets/_TEMPLATE.md`
- 記入済みの実例: `apps/web/AGENTS.md`（Next.js + Vercel 構成）
